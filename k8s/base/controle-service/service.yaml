apiVersion: v1
kind: Service
metadata:
  name: controle-service
  namespace: btp-app
spec:
  selector:
    app: controle-service
  ports:
    - protocol: TCP
      port: 8083
      targetPort: 8083
  type: ClusterIP